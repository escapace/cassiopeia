# @cassiopeia/vue

The Vue integration for Cassiopeia. It installs a shared Cassiopeia instance into a Vue app and exposes `useCassiopeia()` so components can register the runtime CSS variables they depend on.
